name: iron
root: <%= ENV["BABY"] %>
pre_window: cd iron/feedcloser
startup_window: scripts
windows:
  - scripts:
      layout: even-horizontal
      panes:
        - cd test && vim
        - cd src && vim 
  - karma:
      - tmux move-window -s 1 -t 9
      - mocha
