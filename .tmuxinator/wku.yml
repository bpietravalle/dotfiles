name: worker-utils
root: <%= ENV["BABY"] %>/workers/utils
startup_window: scripts
windows:
  - utils:
      layout: even-horizontal
      panes:
        - vim
        - vim
  - cli:
      layout: even-horizontal
      panes:
        - vim
        - vim
  - mocha:
      - tmux move-window -s 2 -t 9
