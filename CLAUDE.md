<!-- scope: dotfiles -->

# dotfiles

> Org standards, `/cc:*` commands, hooks, and agents load globally from the
> user-level `~/.claude/` config. This file carries only dotfiles-specific context.

> Org standards load globally via `~/.claude/CLAUDE.md -> CUSTOMIZATIONS.md -> rvt-ops/standards/claude/manifest*`.
