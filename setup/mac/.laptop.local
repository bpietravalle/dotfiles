#!/bin/bash

#for configuring tmux
gem install tmuxinator

# install XCode's Commandline tools
# required for nvm install
xcode-select --install
curl -o- "https://raw.githubusercontent.com/creationix/nvm/v0.33.2/install.sh" | zsh
